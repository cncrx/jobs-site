const host = "http://:3000"

const users = host + "/user/"
const jobs = host + "/jobs/"
const companies = host + "/companies/"

export {
  users as users_url,
  jobs as jobs_url,
  companies as companies_url,
}